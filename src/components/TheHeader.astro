---
import { SITE_NAV } from '../site.config.mjs'
import SwitchButton from './SwitchButton.astro'
import HeaderLink from './HeaderLink.astro';
---

<header class="flex justify-between items-center">
  <nav>
    {SITE_NAV.map(({ url, text }) => (
      <HeaderLink href={url}>{text}</HeaderLink>
    ))}
  </nav>

  <div>
    <SwitchButton size="50px"></SwitchButton>
  </div>
</header>

<style>
  header {
    grid-area: header;
  }
</style>
